//# This is purely for storing server configurations that are exported and available to all plugins and modules.
//# MACRO_CASE is used within configurations! Only use `const` variables.

export const TICK_SPEED = 3;